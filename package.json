{
  "name": "prodify-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "One command. Production-ready SaaS infrastructure injected into your existing codebase.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=packages/injector",
    "test": "npm run test --workspace=packages/injector",
    "typecheck": "npm run typecheck --workspace=packages/injector && npm run typecheck --workspace=apps/web",
    "dev:web": "npm run dev --workspace=apps/web"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
